<Page x:Class="Transmogrify.Pages.ProjectOverview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Transmogrify - Project"
    d:DesignHeight="450" d:DesignWidth="800"
>
    <DockPanel>
        <Ribbon DockPanel.Dock="Top">
            <Ribbon.ApplicationMenu>
                <RibbonApplicationMenu Visibility="Collapsed"/>
            </Ribbon.ApplicationMenu>
            
            <RibbonTab Header="Project tab">
                <RibbonGroup Header="Project">
                    <RibbonButton Label="New" KeyTip="N" />
                    <RibbonButton Label="Open" KeyTip="O" />
                    <RibbonButton Label="Save" KeyTip="S" />
                    <RibbonButton Label="Exit" Click="ButtonExit_Click" />
                </RibbonGroup>
                <RibbonGroup Header="Misc">
                    <RibbonButton Label="External libraries" KeyTip="L" />
                    <RibbonButton Label="Run project" KeyTip="R" />
                </RibbonGroup>
                <RibbonGroup Header="Endpoints">
                    <RibbonButton Label="Add" KeyTip="A" />
                    <RibbonButton Label="Edit" />
                    <RibbonButton Label="Delete" />
                </RibbonGroup>
                <RibbonGroup Header="Mappings">
                    <RibbonButton Label="Add" KeyTip="M" />
                    <RibbonButton Label="Edit" />
                    <RibbonButton Label="Delete "/>
                </RibbonGroup>
            </RibbonTab>
        </Ribbon>

        <Canvas x:Name="projectCanvas" SizeChanged="ProjectCanvas_SizeChanged" />
    </DockPanel>
</Page>
